# Immich — copy to .env and fill in. Never commit the real .env.
IMMICH_VERSION=release

# Postgres
DB_USERNAME=immich
DB_PASSWORD=change-me
DB_DATABASE_NAME=immich
DB_HOSTNAME=immich_postgres

# Redis
REDIS_HOSTNAME=immich_redis
